# 2026-05-12-js-port

- Worked on: shifted scope from self-contained HTML page to JavaScript conversion of `docs/musicpad.perl`.
- Pending: implement `musicpadToMidi` JS function and verify with sample songs.
- Completed: moved JS source from project root to `src/musicpad.js`; verified CommonJS import and MIDI header output.
- Completed: generated `docs/songs/sth.mid` from `docs/songs/sth.mpd` using `src/musicpad.js`.
- Completed: generated `docs/songs/MBV-When you sleep.mid` from `docs/songs/MBV-When you sleep.mpd`.
- Decision: keep `musicpadToMidi(source)` return type as `Uint8Array`; best suited for browser Blob downloads and Node file writes.
- Completed: added `src/musicpad.html` with textarea, `./musicpad.js` include, and MIDI download button.
- Verified: download button converts textarea content with `musicpadToMidi`, creates an `audio/midi` Blob, clicks a temporary download link, and revokes URL after delay.
- Decision: develop as separate files in `src/` first; defer single self-contained HTML packaging until functionality is complete and verified.
- Completed: added Play button in `src/musicpad.html` and `src/midi-player.js` Web Audio MIDI scheduler/synth. Verified parser finds note events and HTML wires `player.play(midi)`.
- Completed: Play button now toggles to Stop during playback; Stop calls `player.stop()` and resets UI. Natural playback end resets button to Play.
- Completed: added master volume slider to `src/musicpad.html`; connected it to `MusicpadMidiPlayer.setVolume()` for Web Audio playback.
- Decision: removed in-browser playback scope. Deleted `src/midi-player.js`; `src/musicpad.html` now keeps only textarea and MIDI download.
- Commit prep: initialized git repository on `main`; added `.gitignore` for generated `.mid` files; refreshed `STATE.md` to download-only scope.
- Worked on: optimized `src/musicpad.js` token parsing by gating expensive regex matches with cheap string/character checks.
- Verified: all `docs/songs/*.mpd` fixed-RNG SHA-256 output prefixes stayed unchanged after optimization.
- Result: sample-suite runtime improved from about 729 ms to about 404 ms in Node; instrumented regex `match` calls dropped from millions to ~75k.
- Completed: added `tests/musicpad.test.js` covering inline examples, all sample songs, fixed-RNG MIDI SHA-256 hashes, and MIDI chunk/header validity.
- Optimization step: added cached parsing for comma-separated chord/fret lists in `src/musicpad.js`.
- Verified after optimization: `node -c src/musicpad.js` and `node tests/musicpad.test.js` pass. Not committed.
- Optimization step: replaced common command regex parsing with prefix/slice parsing for strum, tom/guitar mode, stress/soft, loose/velvar, ctrl/sysex, pitch, channel, instrument, ratio, duty, velocity, transpose, and temporary note commands.
- Verified after prefix parsing optimization: `node -c src/musicpad.js` and `node tests/musicpad.test.js` pass. Not committed.
- Optimization step: track preprocessing now tokenizes with a scanner and merge rules instead of the remaining x/dash/equal recompact regex passes.
- One tokenizer attempt initially changed `melo1.mpd`; fixed dash handling to avoid joining flat notes across original whitespace while preserving `nt-1`, `B-5`, `B-==`, and guitar chord dash cases.
- Verified after tokenizer optimization: `node -c src/musicpad.js` and `node tests/musicpad.test.js` pass. Instrumented replace calls dropped to ~80k; warm sample-suite run observed around 166-190 ms. Not committed.
- Updated `src/musicpad.html` default textarea content to `docs/songs/MBV-When you sleep.mpd`; regression tests still pass.
- Optimization step: converted built-in `GUITAR_CHORDS` and `KEY_CHORDS` from Perl-style string tables to direct JS object literals with arrays/nulls.
- Removed `chordMap()` startup parser and changed named keyboard chords to use stored arrays directly; named guitar chords now pass stored fret arrays directly.
- Verified after chord-table conversion: `node -c src/musicpad.js` and `node tests/musicpad.test.js` pass. Not committed.
- Completed: added `Load` button to `src/musicpad.html`; it opens a local file picker and reads the selected text file into the textarea. Verified HTML wiring and regression tests.
- Updated `src/musicpad.html` layout: textarea now flexes with viewport height and uses smaller page/textarea border spacing.
- Updated `src/musicpad.html`: moved Load/Generate controls into a fixed toolbar above the textarea, aligned to the right.
- Updated `src/musicpad.html`: title and toolbar now share one header row, with Musicpad left and controls right.
- Completed: added `Save` button between Load and Generate; downloads textarea source as `musicpad.mpd`.
- Added native tooltips to Load, Save, and Generate buttons explaining each action.
- Completed: created `src/reference.html` as a complete language reference from the old help content, reorganized for the current app.
- Completed: created `src/tutorial.html` as a step-by-step tutorial from the old tutorial content.
- Updated `src/musicpad.html` header with Tutorial and Reference links; verified docs links and regression tests.
- Updated `src/musicpad.html`: bundled Editor, Tutorial, and Reference into one page. Top page buttons now switch visible sections; editor-only Load/Save/Generate toolbar hides outside the editor page.
- Verified bundled page structure and regression tests. Standalone `src/tutorial.html` and `src/reference.html` were left in place pending explicit removal.
- Updated bundled `src/musicpad.html` header layout: Editor/Tutorial/Reference buttons now sit next to the Musicpad label on the left; Load/Save/Generate remain editor-only on the right.
- Updated `src/musicpad.html`: app now uses fixed-height viewport layout; header stays visible while Tutorial/Reference page content scrolls independently.
- Added dynamic `Generate MIDI` buttons below each Tutorial code block; each compiles that example and downloads `musicpad-example-N.mid`.
- Refactored `src/musicpad.html` download logic through shared `downloadBlob()` and `generateMidiDownload()` helpers. Regression tests pass.
- Removed success/status messages from Load, Save, Generate, and tutorial example generation; errors still display.
- Fixed Tutorial/Reference code block alignment in bundled `src/musicpad.html` by removing generated HTML indentation inside `<pre>` contents. Regression tests pass.
- Replaced separate Editor/Tutorial/Reference buttons with a single page selector button and dropdown menu. Selector label updates to current page; menu lists the other pages.
- Added bundled About page after Reference in `src/musicpad.html`; selector now includes About. About content summarizes the original Loïc Prot/CGI Perl Musicpad, polymath.cgi inspiration, capabilities, the Meshuggah “I” project, and the JavaScript port/contact note for Remo Dentato.
