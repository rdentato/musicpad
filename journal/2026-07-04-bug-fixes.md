# 2026-07-04 Bug fixes

- Started from `BUG-TO-FIX.md` non-gated tasks.
- Baseline `node tests/musicpad.test.js` passes before changes.
- Existing worktree had modified `PLAN.md`/`STATE.md`, untracked `BUG-TO-FIX.md`, `old/`, and `xxx/`; will avoid unrelated edits and leave `old/`/`xxx/` contents untouched.

- Implemented engine fixes: macro bound errors, loose/velvar defaults and validation, chained repeat coalescing, short minor chord aliases, title/author extraction before comment stripping, adjacency-aware dash/rest tokenization, unknown instrument errors, channel/nt/tempo/resolution validation, token/source error context, and UTF-8 MIDI meta text.
- Implemented app/docs housekeeping: tutorial examples preserve current volume, status is outside the editor page, README typo/example fixes, `.gitignore` ignores `old/` and `xxx/`.
- Added regression tests for the requested engine cases and UTF-8 title meta bytes.
- Rebuilt `src/musicpad.html`, refreshed `docs/index.html`, and confirmed they match.
- Verification: `node tests/musicpad.test.js` passes after changes; bundled-song hashes were not updated. Gated MAJ7/AUG compatibility changes were left untouched.
- Pending: browser smoke test was not run in this harness.
- Follow-up: added nearby-source context to preprocessing repeat/macro error paths, rebuilt again, and re-ran tests successfully.
